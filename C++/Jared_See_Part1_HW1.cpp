#include<iostream>
using namespace std;


int gcd(int x, int y);
int main()
{
    int x, y;
    cout << "Enter two values: ";
    cin >> x >> y;
    cout << gcd(x,y);
}

int gcd(int x, int y)
{
    while()
}